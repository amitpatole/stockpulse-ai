// Shared UI constants for the performance comparison feature.
// Single source of truth — import from here, never redefine locally.
export const COMPARISON_PALETTE = ['#f59e0b', '#10b981', '#ef4444', '#8b5cf6'] as const;
